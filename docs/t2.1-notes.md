# T2.1 — Install Consent Flow + Default Device Detection

Branch scaffold. Implementation tracked in the T2.1 issue.

## Scope

- On APO-not-installed: show consent dialog naming the actual default output device.
- Decline path → exit gracefully, zero system changes.
- No active output device → clear message → clean exit.
- State-machine flow + dialogs; **no installation logic yet** (stub only — that is T2.2).

## NEEDS-VM-VERIFICATION

- "No active output device" detection edge cases (disabled vs. unplugged states).
- Consent dialog correctly names the default device on machines with multiple output devices.
